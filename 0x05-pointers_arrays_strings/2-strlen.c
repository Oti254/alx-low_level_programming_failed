#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 *
 * @s: characters in string
 *
 * Return: 0
 *
 */
int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
