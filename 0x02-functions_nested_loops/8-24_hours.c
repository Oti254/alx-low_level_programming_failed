#include "main.h"

/**
 * jack_bauer - prints every minute of the day
 *
 * Return: ...
 *
 */
void jack_bauer(void)
{
	int h, m;

	for (h = 0; h <= 24; h++)
	{
		for (m = 00; m <= 59; m++)
		{
			return (m);
		}
		return (h);
	}
}
