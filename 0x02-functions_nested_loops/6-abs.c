#include "main.h"

/**
 * _abs - computes absolute value
 *
 * @n: integer checked
 *
 * Return: Always 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = -n;
		return (n);
	}
	else
	{
		return (n);
	}
	return (0);
}
